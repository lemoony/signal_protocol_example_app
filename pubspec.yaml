name: signal_protocol_example_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  libsignal_protocol_dart:
    git:
      url: git@github.com:lemoony/libsignal_protocol_dart.git
      ref: feature/web-support-with-overwrites
  js: ^0.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
